pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Jenkins webhook build is working'
                sh 'ls -la'
            }
        }
    }
}
